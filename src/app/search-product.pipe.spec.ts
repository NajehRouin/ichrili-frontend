import { SearchProductPipe } from './search-product.pipe';

describe('SerachProductPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProductPipe();
    expect(pipe).toBeTruthy();
  });
});
