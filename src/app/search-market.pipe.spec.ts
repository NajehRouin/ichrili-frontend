import { SearchMarketPipe } from './search-market.pipe';

describe('SearchMarketPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMarketPipe();
    expect(pipe).toBeTruthy();
  });
});
